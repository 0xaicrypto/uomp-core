{
  "name": "uomp-monorepo",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "User-Owned Memory Protocol - MVP implementation",
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm --filter @uomp/server dev",
    "cli": "node ./packages/cli/dist/cli.js",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.5.0"
  },
  "engines": {
    "node": ">=22.0.0"
  },
  "packageManager": "pnpm@9.0.0"
}
